<p style="color: green"><%= notice %></p>

<%= render @order %>

<div>
  <%= link_to "Edit this order", edit_order_path(@order) %> |
  <%= link_to "Back to orders", orders_path %>
    <%= button_to "Destroy this order", @order, method: :delete %>

  <hr>
  <%= button_to "Jdu balit zasilku", set_state_order_path(order_id: @order.id) %>
  <%= button_to "zasilka hotovo, odeslana", set_state_order_path(order_id: @order.id, create_tracking_number: true) %>
</div>
